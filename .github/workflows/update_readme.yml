name: Update README

on:
  push:
    branches: ["main"]
  # schedule:
  #   - cron: '0 */6 * * *'

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_ACTIONS_TOKEN }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install curl_cffi requests tqdm beautifulsoup4

      - name: Run script
        run: |
          python update_README.py

      - name: Push changes and commit
        continue-on-error: true
        run: |
          git config --global user.email "rlatjwls7882@gmail.com"
          git config --global user.name "rlatjwls7882"
          git commit -am "Update README.md"
          git push

      # - name: Trigger parent repo update
      #   env:
      #     GH_TOKEN: ${{ secrets.GH_ACTIONS_TOKEN }}
      #   run: |
      #     curl -L -X POST \
      #       -H "Accept: application/vnd.github+json" \
      #       -H "Authorization: Bearer ${GH_TOKEN}" \
      #       -H "X-GitHub-Api-Version: 2022-11-28" \
      #       https://api.github.com/repos/rlatjwls7882/Problem-Solving/dispatches \
      #       -d '{"event_type":"submodule-updated","client_payload":{"submodule":"Cpp-Algorithms"}}'
